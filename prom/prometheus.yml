global:
  scrape_interval: 10s
  evaluation_interval: 10s

rule_files:
  - /etc/prometheus/alerts/trustgate-alerts.yml

scrape_configs:
  - job_name: trustgate-app
    metrics_path: /metrics
    static_configs:
      - targets:
          - app:3000

